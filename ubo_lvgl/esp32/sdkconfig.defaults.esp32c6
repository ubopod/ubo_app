# Waveshare ESP32-C6-Touch-AMOLED-1.8.
# NOTE: the ESP32-C6 has 512KB SRAM and NO PSRAM — do not enable CONFIG_SPIRAM.
# Every buffer in this firmware is sized against that; see the memory-budget
# section of esp32/README.md before raising any of them.
CONFIG_ESP_DEFAULT_CPU_FREQ_MHZ_160=y
